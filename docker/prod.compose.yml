services:
  web:
    image: ${IMAGE}
    container_name: website-prod
    restart: unless-stopped
    ports:
      - "80:3000"
    env_file:
      - /opt/website/envs/prod.env
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/health"]
      interval: 15s
      timeout: 3s
      retries: 5
